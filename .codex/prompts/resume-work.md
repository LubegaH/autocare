Goal: start the session with full context and a healthy working state.

Context: newest doc in docs/handoffs/ (if newer than the last recorded
completion), docs/memory.md, the roadmap/task list, `git status`.

Constraints: uncommitted changes from before → list them and ask
commit/stash/discard. On a protected branch with pending work → create
the right feature branch. Run the build quietly; failures are fixed
before anything else (full suite waits for review). Touch nothing until
I say go.

Done when: briefing delivered — last completed, current focus, immediate
next action, queue depth.
