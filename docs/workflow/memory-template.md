# Project Memory — [project name]

Lives at `docs/memory.md` in the repo. This is the project's long-term
memory across sessions and across tools (Claude Code and Codex both read
it). Keep it under ~150 lines: it earns space by preventing repeat
mistakes, not by recording history. Prune on every `$ai-dev-learn` review.

## Facts
- Tier: 1/2/3 · Stack: (link ADR-0001)
- Environments and where they live
- Anything unusual about how this project runs

## Decisions index
One line per ADR: number, title, punchline. Full ADRs in docs/decisions/.

## Conventions that aren't obvious from the code
Only the ones an agent would get wrong without being told.

## What the agent gets wrong on this project
The self-correcting list. Add an entry whenever the same mistake happens
twice. Remove entries that haven't triggered in months.
- ...

## Gotchas
Environment quirks, third-party API surprises, "don't touch X without
reading Y first".

## Current focus
What's being built now; link to ROADMAP/task list rather than duplicating.
