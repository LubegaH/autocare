# Handoff: [topic] — YYYY-MM-DD

Save to `docs/handoffs/YYYY-MM-DD-[short-topic].md` in the repo. Never a
temp directory — handoffs are project state, and they must survive the
machine. Reference existing artefacts (PRD, ADRs, ROADMAP, commits) by
path; do not duplicate them. Redact secrets and personal data.

## State
- Branch: · Uncommitted changes: yes/no (what)
- Build: passing/failing · Tests: passing/failing (which)

## Done this session
What was completed, with file references. Terse.

## In flight
What is half-done and exactly where it stopped. The sentence that saves
the next session an hour.

## Next steps (ordered)
1. The very next action, specific enough to start cold
2. ...

## Decisions made (not yet recorded elsewhere)
Decision + reasoning, one line each. Flag any that should become an ADR
or a memory-file entry.

## Gotchas discovered
Anything that cost time and would cost time again.

## For the next agent
Which commands/skills to invoke first (e.g. /resume, read ADR-0003
before touching auth). Which files give fastest context.
